#include "physicsSolvers/fluidFlow/ThermalCompositionalMultiphaseFVMKernels.hpp"

// using DirichletFaceBasedAssemblyKernelPolicy = @DirichletFaceBasedAssemblyKernelPolicy@;

namespace geos
{
namespace thermalCompositionalMultiphaseFVMKernels
{
  template class @NAME@ < @NCOMP@, @NDOF@, @KERNEL_TYPE@ >;
  // these kernels don't implment their own launch, they rely on the isothermal launch which is
  // instantiated as a byproduct of the above explicit instantiation
}

}