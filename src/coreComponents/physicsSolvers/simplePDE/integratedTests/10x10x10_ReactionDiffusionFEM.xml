<!-- This solves a manufactured problem, Lap(U) + U = F, with U = 0 in all the walls. F is constructed such that the analytical solution is simply U(x,y,z) = xyz(1-x)(1-y)(1-z)-->
<Problem>

  <Solvers>
    <ReactionDiffusionFEM name="ReactionDiffusion"
                discretization="FE1"
                timeIntegrationOption="SteadyState"
                fieldName="Temperature"
                verboseLevel="0"
                targetRegions="{Region1}">
    </ReactionDiffusionFEM>
  </Solvers>

  <Mesh>
    <InternalMesh name="mesh1"
                  elementTypes="{C3D8}"
                  xCoords="{0, 1}"
                  yCoords="{0, 1}"
                  zCoords="{0, 1}"
                  nx="{10}"
                  ny="{10}"
                  nz="{10}"
                  cellBlockNames="{cb1}"/>
  </Mesh>

  <Events maxTime="1.0">
    <!-- This event is applied every cycle, and overrides the
    solver time-step request -->
    <PeriodicEvent name="solverApplications"
                   forceDt="1.0"
                   target="/Solvers/ReactionDiffusion" />

    <!-- This event is applied every 1.0s.  The targetExactTimestep
    flag allows this event to request a dt modification to match an
    integer multiple of the timeFrequency. -->
    <PeriodicEvent name="outputs"
                   timeFrequency="1.0"
                   targetExactTimestep="1"
                   target="/Outputs/VTKOutput" />

    <PeriodicEvent name="restarts"
                   timeFrequency="1e99"
                   targetExactTimestep="0"
                   target="/Outputs/restartOutput"
                   endTime="-1" />
  </Events>

  <NumericalMethods>
    <BasisFunctions>
      <LagrangeBasis3 name="linearBasis"  degree = "1"  />
    </BasisFunctions>
    <QuadratureRules>
      <GaussQuadrature3 name="gaussian"   degree="2"  />
    </QuadratureRules>
    <FiniteElements>
      <FiniteElementSpace name="FE1" parentSpace="C3D8" basis="linearBasis" quadrature="gaussian" />
    </FiniteElements>
  </NumericalMethods>

  <ElementRegions>
    <CellElementRegion name="Region1" cellBlocks="{cb1}" materialList="{shale}" />
  </ElementRegions>

  <Constitutive>
    <LinearElasticIsotropic name="granite"
                            defaultDensity="2700"
                            defaultBulkModulus="5.5556e9"
                            defaultShearModulus="4.16667e9"/>
    <LinearElasticIsotropic name="shale"
                            defaultDensity="2700"
                            defaultBulkModulus="5.5556e9"
                            defaultShearModulus="4.16667e9"/>
  </Constitutive>

  <FieldSpecifications>
    <FieldSpecification name="wallX0Term"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0"
               setNames="{wallX0}"/>
    <FieldSpecification name="wallX1Term"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0"
               setNames="{wallX1}"/>
    <FieldSpecification name="wallY0Term"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0"
               setNames="{wallY0}"/>
    <FieldSpecification name="wallY1Term"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0"
               setNames="{wallY1}"/>
    <FieldSpecification name="wallZ0Term"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0"
               setNames="{wallZ0}"/>
    <FieldSpecification name="wallZ1Term"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0"
               setNames="{wallZ1}"/>
  </FieldSpecifications>

  <Outputs>
    <VTK name="VTKOutput" parallelThreads="32" plotFileRoot="plot"/>
    <Restart name="restartOutput"/>
  </Outputs>

  <Geometry>
    <Box name="wallX0" xMin="-0.01, -0.01, -0.01" xMax="+0.01, +1.01, +1.01"/>
    <Box name="wallX1"   xMin="+0.99, -0.01, -0.01" xMax="+1.01, +1.01, +1.01"/>
    <Box name="wallY0" xMin="-0.01, -0.01, -0.01" xMax="+1.01, +0.01, +1.01"/>
    <Box name="wallY1"   xMin="-0.01, +0.99, -0.01" xMax="+1.01, +1.01, +1.01"/>
    <Box name="wallZ0" xMin="-0.01, -0.01, -0.01" xMax="+1.01, +1.01, +0.01"/>
    <Box name="wallZ1"   xMin="-0.01, -0.01, 0.99" xMax="+1.01, +1.01, +1.01"/>
  </Geometry>

</Problem>
