
<Problem>

  <Solvers>
    <LaplaceFEM name="laplace"
                discretization="FE1"
                timeIntegrationOption="SteadyState"
                fieldName="Temperature"
                logLevel="1"
                targetRegions="{Region1}">
    </LaplaceFEM>
  </Solvers>

  <Mesh>
    <PAMELAMeshGenerator name = "Wedge"
                         file = "wedge.msh"/>
  </Mesh>

  <Events maxTime="2.0">
    <!-- This event is applied every cycle, and overrides the
    solver time-step request -->
    <PeriodicEvent name="solverApplications"
                   forceDt="1.0"
                   target="/Solvers/laplace" />

    <!-- This event is applied every 1.0s.  The targetExactTimestep
    flag allows this event to request a dt modification to match an
    integer multiple of the timeFrequency. -->
    <PeriodicEvent name="vtkOutput"
                   timeFrequency="1.0"
                   targetExactTimestep="1"
                   target="/Outputs/wedgeLaplace_VTKOutput" />

    <PeriodicEvent name="siloOutputs"
                   timeFrequency="1.0"
                   targetExactTimestep="1"
                   target="/Outputs/wedgeLaplace_siloOutput" />

    <PeriodicEvent name="restarts"
                   timeFrequency="1.0"
                   targetExactTimestep="0"
                   target="/Outputs/restartOutput" />
  </Events>

  <NumericalMethods>
    <FiniteElements>
      <FiniteElementSpace name="FE1" order="1" />
    </FiniteElements>
  </NumericalMethods>

  <ElementRegions>
    <CellElementRegion name="Region1" cellBlocks="{0_HEX, 1_HEX, 2_HEX, 3_HEX}" materialList="{shale}" />
  </ElementRegions>

  <Constitutive>
    <LinearElasticIsotropic name="granite"
                            defaultDensity="2700"
                            defaultBulkModulus="5.5556e9"
                            defaultShearModulus="4.16667e9"/>
    <LinearElasticIsotropic name="shale"
                            defaultDensity="2700"
                            defaultBulkModulus="5.5556e9"
                            defaultShearModulus="4.16667e9"/>
  </Constitutive>

  <FieldSpecifications>
    <FieldSpecification name="sourceTerm"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="1.0"
               setNames="{source}"/>
    <FieldSpecification name="sinkTerm"
               fieldName="Temperature"
               objectPath="nodeManager"
               scale="0.0"
               setNames="{sink}"/>
  </FieldSpecifications>

  <Outputs>
    <VTK name="wedgeLaplace_VTKOutput" parallelThreads="1" plotFileRoot="plot"/>
    <Silo name="wedgeLaplace_siloOutput" parallelThreads="32" plotFileRoot="plot"/>
    <Restart name="restartOutput"/>
  </Outputs>

  <Geometry>
    <Box name="source" xMin="-0.01, -0.01, -0.01" xMax="+0.01, +1.61, +2.01"/>
    <Box name="sink"   xMin="3.99, -4.01, -0.01" xMax="+4.01, +5.61, +2.01"/>
  </Geometry>

  <Functions>
    <TableFunction name="DirichletTimeFunction"
               inputVarNames="{time}"
               coordinates="{0.0, 1.0, 2.0}"
               values="{0.0, 3.e2, 4.e3}" />
  </Functions>

</Problem>
