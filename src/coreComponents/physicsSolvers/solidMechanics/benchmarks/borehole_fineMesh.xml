<?xml version="1.0" ?>

<Problem>
  <!-- SPHINX_Gravity -->
  <Solvers
    gravityVector="0.0, 0.0, 0.0">
    <!-- SPHINX_GravityEnd -->
    <!-- SPHINX_SolidMechanicsSolver -->
    <SolidMechanicsLagrangianSSLE
      name="lagsolve"
      timeIntegrationOption="QuasiStatic"
      discretization="FE1"
      logLevel="0"
      targetRegions="{ Region2 }"
      solidMaterialNames="{ shale }">
      <!-- SPHINX_SolidMechanicsSolverEnd -->
      <NonlinearSolverParameters
        newtonTol="1.0e-6"
        newtonMaxIter="8"/>
      <LinearSolverParameters
        solverType="direct"
        directParallel="0"
        logLevel="0"/>
    </SolidMechanicsLagrangianSSLE>
  </Solvers>

  <!-- SPHINX_BoreholeMesh -->
  <Mesh>
    <PAMELAMeshGenerator 
      name="mesh1"
      file="borehole_fineMesh.msh"/>
  </Mesh>
  <!-- SPHINX_BoreholeMeshEnd -->



  <Geometry>
    <Box 
      name="all" 
      xMin="-0.00001, -0.00001, -0.00001"
      xMax=" 1.00001,  1.00001,  1.00001"/>

    <Box 
      name="xneg" 
      xMin="-0.00001, -0.00001, -0.00001"
      xMax=" 0.00001,  1.00001,  1.00001"/>

    <Box 
      name="xpos" 
      xMin="0.99999, -0.00001, -0.00001"
      xMax=" 1.00001,  1.00001,  1.00001"/>

    <Box 
      name="yneg" 
      xMin="-0.00001, -0.00001, -0.00001"
      xMax=" 1.00001,  0.00001,  1.00001"/>

    <Box 
      name="ypos" 
      xMin="-0.00001, 0.99999, -0.00001"
      xMax=" 1.00001, 1.00001,  1.00001"/>

    <Box 
      name="zneg" 
      xMin="-0.00001, -0.00001, -0.00001"
      xMax=" 1.00001,  1.00001,  0.00001"/>

    <Box 
      name="zpos" 
      xMin="-0.00001, -0.00001, 0.99999"
      xMax=" 1.00001,  1.00001, 1.00001"/>														
  </Geometry>

  <Events
    maxTime="10.0">
    <!-- This event is applied every cycle, and overrides the
    solver time-step request -->
    <!-- SPHINX_Execution -->
    <PeriodicEvent
      name="solverApplications"
      forceDt="1.0"
      target="/Solvers/lagsolve"/>

    <!-- SPHINX_ExecutionEnd -->
    <!-- This event is applied every 5.0e-5s.  The targetExactTimestep
    flag allows this event to request a dt modification to match an
    integer multiple of the timeFrequency. -->
    <PeriodicEvent
      name="outputs"
      timeFrequency="1.0"
      targetExactTimestep="1"
      target="/Outputs/siloOutput"/>

    <!--PeriodicEvent
      name="restarts"
      timeFrequency="1e99"
      targetExactTimestep="0"
      target="/Outputs/restartOutput"/-->
  </Events>

  <!-- SPHINX_FEDiscretization -->
  <NumericalMethods>
    <FiniteElements>
      <FiniteElementSpace
        name="FE1"
        order="1"/>
    </FiniteElements>
  </NumericalMethods>

  <!-- SPHINX_FEDiscretizationEnd -->
  <ElementRegions>
    <CellElementRegion
      name="Region2"
      cellBlocks="{ Omega_HEX }"
      materialList="{ shale }"/>
  </ElementRegions>

  <Constitutive>
    <!-- SPHINX_Material -->
    <ElasticIsotropic
      name="shale"
      defaultDensity="2700"
      defaultBulkModulus="5.5556e9"
      defaultShearModulus="4.16667e9"/>

    <!-- SPHINX_MaterialEnd -->
  </Constitutive>

  <!-- SPHINX_BoundaryConditions -->
  <FieldSpecifications>
    <FieldSpecification
      name="xconstraint"
      objectPath="nodeManager"
      fieldName="TotalDisplacement"
      component="0"
      scale="0.0"
      setNames="{ xneg, xpos }"/>

    <FieldSpecification
      name="yconstraint"
      objectPath="nodeManager"
      fieldName="TotalDisplacement"
      component="1"
      scale="0.0"
      setNames="{ yneg, ypos }"/>

    <FieldSpecification
      name="zconstraint"
      objectPath="nodeManager"
      fieldName="TotalDisplacement"
      component="2"
      scale="0.0"
      setNames="{ zneg, zpos }"/>

    <FieldSpecification
      name="borehole stress"
      objectPath="faceManager"
      fieldName="Traction"
      component="-1"
      scale="-1.0e6"
      functionName="timeFunction"
      setNames="{ boreholeWall }"/>
  </FieldSpecifications>

  <!-- SPHINX_BoundaryConditionsEnd -->
  <!-- SPHINX_TableFunction -->
  <Functions>
    <TableFunction
      name="timeFunction"
      inputVarNames="{ time }"
      coordinates="{ 0.0, 10.0 }"
      values="{ 0.0, 10.0 }"/>
  </Functions>

  <!-- SPHINX_TableFunctionEnd -->
  <Outputs>
    <Silo
      name="siloOutput"
      parallelThreads="32"
      plotFileRoot="plot"
      childDirectory="sub"/>

    <Restart
      name="restartOutput"/>
  </Outputs>
</Problem>
