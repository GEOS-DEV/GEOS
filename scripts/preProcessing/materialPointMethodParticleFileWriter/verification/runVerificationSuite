#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
PFW_ROOT="$(cd "$SCRIPT_DIR/.." && pwd -P)"
PYTHON_CMD="${PFW_PYTHON:-${PFW_PYTHON_COMMAND:-/usr/tce/bin/python3}}"
exec "$PYTHON_CMD" "$PFW_ROOT/mpm_vv_suite.py" --suite verification "$@"
