# MaterialSwap event verification

This folder verifies a single MaterialSwap event with stationary kinematics.  The quantitative post-process checks that a center tracer remains fixed while its `particleMaterialType` changes from source material 0 to destination material 1 at the event time.
