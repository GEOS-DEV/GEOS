#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  include:
  
  # System-specific config files
  - ../../../scripts/spack_configs/toss_3_x86_64_ib/compilers.yaml
  - ../../../scripts/spack_configs/toss_3_x86_64_ib/packages.yaml


  # General configurations
  - ../../../scripts/spack_configs/config.yaml

  # geosx specific package recipes to use over spack builtin
  repos:
  - ../../../scripts/spack_packages
  view: false
  specs:
  - geosx@develop%clang@10.0.1
  bootstrap:
    trusted:
      github-actions-v0.1: false
  config:
    install_tree:
      root: $spack/../${SYS_TYPE}_packages
    concretizer: clingo
