<?xml version="1.0" ?>

<Problem>
  <Solvers>
    <!-- define the solver -->
    <!-- we'll have to discuss the cflFactor with Randy -->
    <!-- define the source coordinates -->
    <!-- define the time source frequency -->
    <!-- define the receiver coordinates -->
       <AcousticSEM
      name="acousticSolver"
      cflFactor="0.8"
      discretization="FE1"
      targetRegions="{Region}"
      sourceCoordinates="{{500.1,500.1,500.1}}"
      timeSourceFrequency="5.0"
      receiverCoordinates="{{750.1,750.1,750.1}}"
      dtSeismoTrace="0.001" />
  </Solvers>

  <!-- hexahedral mesh generated with vtk from regular IJK grid -->
  <Mesh>
    <VTKMesh
        name="mesh"
        fieldsToImport="{mediumVelocity}"
        fieldNamesInGEOSX="{mediumVelocity}"
        partitionRefinement="0"
        file="tiny.vtu"/>
  </Mesh>
  <!--file="../1.models/32Blocks_globalId/vel.pvtu"/>-->

  <Geometry>
    <!-- tag the free surface boundary using box -->
    <Box
      name="zpos"
      xMin="{ -0.01, -0.01, -0.01}"
      xMax="{ 15000.01, 15000.01, 0.11 }"/>
  </Geometry>

  <!-- list of events-->
  <Events maxTime="0.01">
    <!-- trigger the application of the solver -->
    <!-- control the timestepping here with forceDt -->
    <!-- (there are other ways to control the time step size) -->
    <PeriodicEvent
      name="solverApplications"
      forceDt="0.001"
      targetExactStartStop="0"
      targetExactTimestep="0"
      target="/Solvers/acousticSolver"/>
  </Events>

  <!-- numerical methods for discretization-->
  <NumericalMethods>
    <FiniteElements>
      <FiniteElementSpace
        name="FE1"
        order="1"
        formulation="SEM"/>
    </FiniteElements>
  </NumericalMethods>

  <!-- Region -->
  <ElementRegions>
    <CellElementRegion
      name="Region"
      cellBlocks="{ hexahedra }"
      materialList="{ nullModel }"/>
  </ElementRegions>

  <Constitutive>
    <NullModel
      name="nullModel"/>
  </Constitutive>

  <!-- here use FieldSpecification to define: -->
  <FieldSpecifications>
    <FieldSpecification
      name="zposFreeSurface"
      objectPath="faceManager"
      fieldName="FreeSurface"
      scale="0.0"
    setNames="{zpos}"/>
  </FieldSpecifications>

</Problem>
